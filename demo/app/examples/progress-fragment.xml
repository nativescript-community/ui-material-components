<StackLayout xmlns:mdp="nativescript-material-progress" padding="20">
    <mdp:Progress width="100%" value="{{ progressValue }}"  maxValue="{{ progressMaxValue }}" loaded="onProgressLoaded"/>
</StackLayout>