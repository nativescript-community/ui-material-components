<StackLayout xmlns:mdcp="nativescript-material-components/progress" padding="20">
    <mdcp:Progress width="100%" value="{{ progressValue }}"  maxValue="{{ progressMaxValue }}" loaded="onProgressLoaded"/>
</StackLayout>